import Vehicle from "./Vehicle";

export default class PlayerCar extends Vehicle {
    constructor() {
        super("PlayerCar");
    }
    update(deltaTime) {
        super.update(deltaTime);
    }
}